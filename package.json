{
  "name": "olg-concept",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Oviedo Law Group concept website by Kodaxa \u2014 review only",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs --dev",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "node --check public/app.js && node --check scripts/build.mjs && npm test"
  }
}
