import {values} from '../data.mjs';
import {image,imageCredit,scene} from '../imagery.mjs';
import {icon} from '../icons.mjs';
import {eyebrow,subhero,button,cta} from '../components.mjs';
export const about=()=>`${subhero('The firm','An established identity.<br><em>A clear point of view.</em>','A Clovis law-firm concept that keeps the seriousness of the existing identity and makes the experience more direct.','civic')}<section class="section shell about-intro"><div>${eyebrow('Rooted in Clovis')}<h2>Representation<br>is always<br><em>personal.</em></h2></div><div><p class="lead">Behind every matter is a person, a livelihood, and a next chapter. The firm’s website should make it easier to find the right conversation—not stand in the way of it.</p><p>This proposed presentation brings the attorneys, practice areas, and office information into one clear experience. The underlying content remains subject to the firm’s review.</p>${button('Meet our attorneys','/attorneys/','outline')}</div></section><div class="shell about-scene">${scene('consultation')}</div><section class="values-section values-section--photo">${image('valley',{className:'section-media',decorative:true})}<div class="shell"><div class="values-grid">${values.map(v=>`<article>${icon(v.icon)}<h3>${v.title}</h3><p>${v.copy}</p></article>`).join('')}</div></div>${imageCredit('valley')}</section><section class="section shell process-section"><div>${eyebrow('How a conversation begins')}<h2>A straightforward<br><em>starting point.</em></h2></div><ol class="process-list"><li><span>01</span><div><h3>Contact the office.</h3><p>Ask about the firm’s current services and its process for new inquiries.</p></div></li><li><span>02</span><div><h3>Discuss the next step.</h3><p>The office can explain what information it needs and how to provide it appropriately.</p></div></li><li><span>03</span><div><h3>Confirm the relationship.</h3><p>Contact alone does not establish representation. Engagement terms and acceptance must be confirmed with the firm.</p></div></li></ol></section>${cta()}`;
