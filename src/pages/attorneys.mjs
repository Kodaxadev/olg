import {attorneys} from '../data.mjs';
import {e,eyebrow,subhero,attorneyCards,portrait,button,cta,reviewNote} from '../components.mjs';
export const attorneyIndex=()=>`${subhero('Our attorneys','The people behind<br><em>the practice.</em>','A direct connection to the people who will help you take the next step.')}<section class="section shell attorney-list"><div class="section-heading"><div>${eyebrow('Counsel. With a name.')}<h2>Meet Oviedo<br><em>Law Group.</em></h2></div><p>This concept reflects a two-attorney structure.<br>Final biographies and portraits require firm approval.</p></div><div class="attorney-index-grid">${attorneyCards()}</div>${reviewNote('Monograms are intentional placeholders. The generated portraits in the original mockups are not photographs of these attorneys.')}</section>${cta()}`;
export function attorneyProfile(slug){
 const a=attorneys.find(x=>x.slug===slug);if(!a)throw new Error(`Unknown attorney: ${slug}`);
 return `${subhero('Attorney profile',e(a.name),a.intro)}<section class="section shell profile-grid"><aside>${portrait(a)}<div class="profile-contact">${button('Contact the office','/contact/','outline')}</div></aside><article class="profile-copy">${eyebrow(a.role)}<h2>Professional<br><em>background.</em></h2><p class="lead">${e(a.bio)}</p><dl class="credentials">${a.credentials.map(([t,d])=>`<div><dt>${e(t)}</dt><dd>${e(d)}</dd></div>`).join('')}</dl>${reviewNote(a.review)}<a class="text-link" href="${e(a.source)}" target="_blank" rel="noopener noreferrer">${e(a.sourceLabel)} <span aria-hidden="true">↗</span><span class="sr-only"> (opens in a new tab)</span></a><p class="small-note">Source link supplied for content review, not a statement of current credential verification.</p></article></section>${cta()}`;
}
